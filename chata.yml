---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

run:
  path: echo
       text block: |
        This is an example of a literal block which preserves the text how
        it 
            may be for whatever 
               reason you want it to 
         be like 
        this.
       
 # this text eill not become part of the output
 
